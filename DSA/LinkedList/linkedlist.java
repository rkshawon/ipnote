int* find middle of linked list
->use two pointer node1 and node2 and traverse to link node1 should jump two time and node should jump one time.
after loop node2 should be in the middle. (while looping with node1 before second jump check if next is null or not ).